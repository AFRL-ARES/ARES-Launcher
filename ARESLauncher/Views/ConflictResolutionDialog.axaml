<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        xmlns:vm="clr-namespace:ARESLauncher.ViewModels"
        x:Class="ARESLauncher.ConflictResolutionDialog"
        x:DataType="vm:ConflictResolutionDialogViewModel"
        Title="Conflict Resolution">
  <StackPanel>
	  <TextBlock>Placeholder for some informational text about what to do about existing running ARES (:</TextBlock>
      <Button Command="{Binding TakeOverCommand}">Take Over</Button>
      <Button Command="{Binding KillCommand}">Kill</Button>
      <Button Command="{Binding IgnoreCommand}">Ignore</Button>
  </StackPanel>
</Window>
